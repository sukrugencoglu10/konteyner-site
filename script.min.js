let dashboardWindow = null;function sendDataToDashboard(category, item, action, extra) {extra = extra || {};const timestamp = new Date().toISOString();if (dashboardWindow && !dashboardWindow.closed) {dashboardWindow.postMessage({source: 'KATALOG_TRACKER',payload: { category, item, action, timestamp: new Date().toLocaleTimeString(), isHotLead: action.includes('HOT LEAD'), ...extra }}, '*');}(window.dataLayer = window.dataLayer || []).push({event: 'user_interaction', event_category: category,event_action: action, event_label: item,event_timestamp: timestamp, is_hot_lead: action.includes('HOT LEAD'), ...extra});}function captureGclid() {const gclid = new URLSearchParams(window.location.search).get('gclid');const gclidField = document.getElementById('gclid_field');if (gclid) {if (gclidField) gclidField.value = gclid;localStorage.setItem('gclid', gclid);} else {const stored = localStorage.getItem('gclid');if (stored && gclidField) gclidField.value = stored;}}function throttle(fn, limit) {let t;return function() { if (!t) { fn.apply(this, arguments); t = true; setTimeout(() => t = false, limit); } };}document.addEventListener('DOMContentLoaded', function() {captureGclid();const navToggle = document.getElementById('navToggle');const navMenu = document.querySelector('.nav-menu');const navbar = document.querySelector('.navbar');if (navToggle && navMenu)
navToggle.addEventListener('click', () => navMenu.classList.toggle('active'));document.querySelectorAll('.nav-menu a').forEach(l =>
l.addEventListener('click', () => { if (navMenu) navMenu.classList.remove('active'); })
);window.addEventListener('scroll', () => {if (navbar) navbar.style.boxShadow = window.pageYOffset > 100 ? '0 4px 6px -1px rgba(0,0,0,.1)' : 'none';});const productCategory = document.getElementById('productCategory');const cargoContainerOptions= document.getElementById('cargo-container-options');const cargoType = document.getElementById('cargoType');const quantityOptions = document.getElementById('quantity-options');const containerQuantity = document.getElementById('containerQuantity');const highTicketForm = document.getElementById('pekconHighTicketForm');if (productCategory && cargoContainerOptions && cargoType) {productCategory.addEventListener('change', function() {const isStandart = this.value === 'Standart_Konteyner';cargoContainerOptions.style.display = isStandart ? 'block' : 'none';isStandart ? cargoType.setAttribute('required', 'required') : cargoType.removeAttribute('required');if (!isStandart) cargoType.value = '';if (quantityOptions) quantityOptions.style.display = this.value ? 'block' : 'none';if (containerQuantity) {this.value ? containerQuantity.setAttribute('required', 'required') : containerQuantity.removeAttribute('required');if (!this.value) containerQuantity.value = '';}sendDataToDashboard('form', 'Ürün: ' + this.options[this.selectedIndex].text, 'seçildi');});}if (highTicketForm) {const submitBtn = highTicketForm.querySelector('button[type="submit"]');highTicketForm.addEventListener('submit', function(e) {e.preventDefault();const lang = document.documentElement.lang || 'tr';const t = lang === 'en'
? { sending: 'Sending...', success: '✅ Request received! Our team will contact you.', error: '⚠️ Error. Please contact us via WhatsApp.', btnNormal: 'SEND' }: { sending: 'Gönderiliyor...', success: '✅ Talebiniz Alındı! Uzmanlarımız size dönüş yapacak.', error: '⚠️ Hata oluştu. WhatsApp\'tan yazın.', btnNormal: 'GÖNDER' };const statusMsg = document.getElementById('statusMessage');const email = document.getElementById('userEmail')?.value || '';const phone = document.getElementById('userPhone')?.value || '';const transaction = document.getElementById('transactionType')?.value || '';const cat = document.getElementById('productCategory')?.value || '';const cargoDetail = document.getElementById('cargoType')?.value || '';if (submitBtn) { submitBtn.disabled = true; submitBtn.innerHTML = t.sending; }(window.dataLayer = window.dataLayer || []).push({event: 'lead_form_submit',user_data: { email, phone_number: phone },lead_details: { transaction_type: transaction, product_category: cat, cargo_type_detail: cargoDetail },value: 1000});const fd = new FormData(highTicketForm);if (cargoDetail) fd.set('cargoType', cargoDetail);fetch('https:
method: 'POST', mode: 'no-cors',headers: { 'Content-Type': 'application/x-www-form-urlencoded' },body: new URLSearchParams(fd).toString()})
.then(() => {if (statusMsg) { statusMsg.innerHTML = t.success; statusMsg.style.color = '#4ade80'; }highTicketForm.reset();setTimeout(() => { if (submitBtn) { submitBtn.innerHTML = t.btnNormal; submitBtn.disabled = false; } }, 3000);})
.catch(() => {if (statusMsg) { statusMsg.innerHTML = t.error; statusMsg.style.color = '#fb7185'; }if (submitBtn) submitBtn.disabled = false;});});}(window.requestIdleCallback || function(cb) { setTimeout(cb, 300); })(function() {sendDataToDashboard('page', 'Ana Sayfa', 'yüklendi', { page_title: document.title });document.addEventListener('keydown', function(e) {if (e.ctrlKey && e.shiftKey && (e.key === 'D' || e.key === 'd')) {e.preventDefault();if (dashboardWindow && !dashboardWindow.closed) { dashboardWindow.focus(); return; }dashboardWindow = window.open('dashboard/index.html', 'PekconDashboard', 'width=1400,height=900,resizable=yes,scrollbars=yes');if (!dashboardWindow) alert('⚠️ Pop-up engelleyici aktif!');}});if (navToggle && navMenu)
navToggle.addEventListener('click', () => sendDataToDashboard('navbar', 'Menu', navMenu.classList.contains('active') ? 'açıldı' : 'kapandı'));document.querySelectorAll('.nav-menu a').forEach(l =>
l.addEventListener('click', function() { sendDataToDashboard('navbar', this.textContent.trim(), 'tıklandı'); })
);const logo = document.querySelector('.logo a');if (logo) logo.addEventListener('click', () => sendDataToDashboard('navbar', 'Logo', 'tıklandı'));const langSwitch = document.querySelector('.lang-switch');if (langSwitch) langSwitch.addEventListener('click', e => {if (e.target.tagName === 'A' || e.target.tagName === 'SPAN')
sendDataToDashboard('navbar', 'Dil: ' + e.target.textContent.trim(), 'tıklandı');});const tx = document.getElementById('transactionType');if (tx) tx.addEventListener('change', function() { sendDataToDashboard('form', 'İşlem: ' + this.options[this.selectedIndex].text, 'seçildi'); });const ct = document.getElementById('cargoType');if (ct) ct.addEventListener('change', function() { sendDataToDashboard('form', 'Konteyner: ' + this.options[this.selectedIndex].text, 'seçildi'); });const lb = document.getElementById('lightbox');const lbImg = document.getElementById('lightbox-img');if (lb && lbImg) {document.querySelectorAll('.product-img').forEach((img, i) => {img.style.cursor = 'zoom-in';img.addEventListener('click', function(e) {e.preventDefault(); e.stopPropagation();lb.style.display = 'block'; lbImg.src = this.src; lbImg.alt = this.alt;document.body.style.overflow = 'hidden';const title = this.alt || 'Görsel ' + (i + 1);sendDataToDashboard('product', title, 'lightbox açıldı');(window.dataLayer = window.dataLayer || []).push({ event: 'image_zoom', image_name: title });});});const closeLb = () => { lb.style.display = 'none'; document.body.style.overflow = 'auto'; };const lbClose = document.querySelector('.lightbox-close');if (lbClose) lbClose.addEventListener('click', closeLb);lb.addEventListener('click', e => { if (e.target === lb) closeLb(); });document.addEventListener('keydown', e => { if (e.key === 'Escape' && lb.style.display === 'block') closeLb(); });}document.querySelectorAll('.product-card').forEach((card, i) => {const title = card.querySelector('h3')?.textContent || 'Ürün ' + (i + 1);let timer = null;card.addEventListener('click', e => { if (!e.target.classList.contains('product-img')) sendDataToDashboard('product', title, 'kart tıklandı'); });card.addEventListener('mouseenter', () => {setTimeout(() => { if (card.matches(':hover')) sendDataToDashboard('product', title, 'ilgileniyor', { dwell_time: 3 }); }, 3000);timer = setTimeout(() => sendDataToDashboard('product', title, '🔥 HOT LEAD', { dwell_time: 10, lead_quality: 'hot' }), 10000);});card.addEventListener('mouseleave', () => { clearTimeout(timer); timer = null; });});const wa = document.querySelector('.whatsapp-float');if (wa) wa.addEventListener('click', () => {sendDataToDashboard('whatsapp', 'WhatsApp', 'tıklandı', { conversion_intent: 'high' });(window.dataLayer = window.dataLayer || []).push({ event: 'conversion', send_to: 'AW-10974974305/3AH9COjd9OcbEOGio_Eo' });if (typeof gtag === 'function') gtag('event', 'conversion', { send_to: 'AW-10974974305/3AH9COjd9OcbEOGio_Eo' });});document.querySelectorAll('.social-link').forEach(l =>
l.addEventListener('click', function() {const a = this.getAttribute('aria-label') || 'Sosyal';sendDataToDashboard('social', a, 'tıklandı', { platform: a });})
);const milestones = { 25: 0, 50: 0, 75: 0, 100: 0 };const sectionNames = { anasayfa: 'Ana Sayfa', hizmetler: 'Hizmetler', urunler: 'Ürünler', hakkimizda: 'Hakkımızda', 'bize-ulasin': 'İletişim' };let lastSection = null;window.addEventListener('scroll', throttle(function() {const scrollTop = window.pageYOffset;const docH = document.documentElement.scrollHeight - window.innerHeight;if (docH <= 0) return;const pct = Math.round(scrollTop / docH * 100);[25, 50, 75, 100].forEach(m => {if (pct >= m && !milestones[m]) { milestones[m] = 1; sendDataToDashboard('scroll', '%' + m + ' scroll', 'ulaşıldı', { scroll_depth: m }); }});let cur = null;Object.keys(sectionNames).forEach(id => {const el = document.getElementById(id);if (el) { const r = el.getBoundingClientRect(); if (r.top <= window.innerHeight / 2 && r.bottom >= window.innerHeight / 2) cur = id; }});if (cur && lastSection !== cur) { lastSection = cur; sendDataToDashboard('scroll', 'Bölüm: ' + sectionNames[cur], 'görüntülendi', { section_name: sectionNames[cur] }); }}, 500));});});